// as variaveis são espaços que fazemos na memoria do PC para armazenamos tipos de dados. pode ser armazenado qualquer tipo de dado
// como: funções, strings, numbers, booleans, objects, arrays e etc.
// porem temos 3 tipos de variaveis. as: var, let, const;

var variavelVar;
let variavelLet;
const variavelConst = 0;

// e todas elas tem suas diferenças como:
/*
    var{
        --não respeita muito scopos locais e é global.
        --pode ter reatribuição de valor;
        --pode iniciar sem um valor definido ainda;
    }

    let{
        --respeita scopos locais;
        --pode ter reatribuições de valor;
        --pode iniciar sem valor definido;
    }

    const{
        --respeita scopos locais;
        --não pode ter reatribuição de valor;
        --não pode iniciar sem nenhum valor;
    } 

*/